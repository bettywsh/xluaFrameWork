import "msgdef.proto";
option optimize_for=LITE_RUNTIME;

//请求发送怪物攻城活动排名信息
message GWSendSiegeRankInfo
{
   optional PACKET_ID PacketId = 1 [default=PACKET_WG_ACTIVITY_GWSendSiegeRankInfo];
   repeated string Name = 2;
}

// 响应发送怪物攻城活动排名信息
message WGRetSendSiegeRankInfo
{
   optional PACKET_ID PacketId = 1 [default=PACKET_WG_ACTIVITY_WGRetSendSiegeRankInfo];
   optional int32 PlayerId = 2;   //PlayerId 
}